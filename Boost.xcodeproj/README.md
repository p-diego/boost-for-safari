# Boost — Safari Web Extension

Boost is a Safari Web Extension inspired by “Boost” on Arc, adapted for Safari. This repository explains how to build the container app locally and share an unsigned ZIP build without the Apple Developer Program.

Important: This approach is intended for testers/developers. Users will need to bypass Gatekeeper on first launch.
